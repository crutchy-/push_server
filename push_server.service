# copy to /etc/systemd/system
# systemctl enable push_server.service

[Unit]
Description=web socket server
After=network.target

[Service]
Type=simple
ExecStart=php /var/include/vhosts/default/inc/push_server/server.php localhost 50000

[Install]
WantedBy=multi-user.target
